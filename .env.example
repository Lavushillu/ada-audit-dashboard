# Jira connection settings
JIRA_URL=https://jira.walmart.com

# Default tester username shown on first load
# Can be changed anytime via the "Change User" button in the dashboard
JIRA_TESTER=l0p0c77

# Optional: PAT or password fallback (not needed if using browser SSO)
# JIRA_USERNAME=your_associate_id
# JIRA_PASSWORD=your_pat_or_password
